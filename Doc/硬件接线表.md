# 硬件接线表

## 1. 数码管接线 (74HC595 模块)

| 模块引脚 | STM32 引脚 | GPIO | 说明 |
|---------|-----------|------|------|
| **DS/SDI** | **PA5** | GPIO_PIN_5 | 串行数据输入 |
| **SHCP/SCLK** | **PA6** | GPIO_PIN_6 | 移位寄存器时钟 |
| **STCP/LOAD** | **PA7** | GPIO_PIN_7 | 输出寄存器时钟 (锁存) |
| **VCC** | 3.3V | - | 电源 |
| **GND** | GND | - | 地 |

---

## 2. 按键接线

| 按键 | 功能 | STM32 引脚 | GPIO | 接线方式 |
|-----|------|-----------|------|---------|
| **BTN1** | SET 确认 | **PB3** | GPIO_PIN_3 | 按键→GND |
| **BTN2** | 加 | **PB4** | GPIO_PIN_4 | 按键→GND |
| **BTN3** | 减 | **PB0** | GPIO_PIN_0 | 按键→GND |
| **BTN4** | 左移/切换 | **PB1** | GPIO_PIN_1 | 按键→GND |

**按键接法**：按键一端接 GPIO，另一端接 **GND**（低电平有效）

---

## 3. LED 指示灯接线

| 引脚 | STM32 引脚 | GPIO | 说明 |
|-----|-----------|------|------|
| **LED** | **PC13** | GPIO_PIN_13 | 超温指示灯 |

**LED 接法**：
- LED 正极 → 限流电阻 (220Ω~1kΩ) → **3.3V**
- LED 负极 → **PC13**
- **低电平点亮**

---

## 4. 串口接线

### UART1 (连接上位机/电脑)

| 功能 | STM32 引脚 | GPIO | 连接 |
|-----|-----------|------|------|
| **TX** | **PA9** | GPIO_PIN_9 | → 上位机 RX |
| **RX** | **PA10** | GPIO_PIN_10 | → 上位机 TX |
| **GND** | GND | - | 共地 |

### UART2 (连接温度模块)

| 功能 | STM32 引脚 | GPIO | 连接 |
|-----|-----------|------|------|
| **TX** | **PA2** | GPIO_PIN_2 | → 温度模块 RX |
| **RX** | **PA3** | GPIO_PIN_3 | → 温度模块 TX |
| **GND** | GND | - | 共地 |

**注意**：
- RS485 模块可能需要 MAX3232 电平转换
- 确保所有设备 **共地 (GND)**

---

## 5. 电源接线

| 引脚 | 说明 |
|-----|------|
| **3.3V** | STM32 核心板供电 |
| **GND** | 系统地 |
| **5V** | 74HC595 模块可用 5V 供电 |

---

## 6. 完整接线图

```
                    STM32F103C8T6 (最小系统板)
                    ┌─────────────────────────┐
                    │                         │
        3.3V ───────┤ VCC                     │
        GND  ───────┤ GND                     │
                    │                         │
        PA5  ───────┤ PA5  ├────→ 74HC595 DS  │
        PA6  ─────── PA6  ├────→ 74HC595 SHCP│
        PA7  ───────┤ PA7  ├────→ 74HC595 STCP│
                    │                         │
        PB0  ───────┤ PB0  ├────→ BTN3 (减)  │
        PB1  ───────┤ PB1  ├────→ BTN4 (移位)│
        PB3  ─────── PB3  ├────→ BTN1 (SET) │
        PB4  ───────┤ PB4  ├────→ BTN2 (加)  │
                    │                         │
        PC13 ─────── PC13 ├────→ LED (-)    │
                    │                         │
        PA2  ───────┤ PA2  ├────→ UART2 TX   │
        PA3  ───────┤ PA3  ├────→ UART2 RX   │
                    │                         │
        PA9  ───────┤ PA9  ├────→ UART1 TX   │
        PA10 ───────┤ PA10 ├────→ UART1 RX   │
                    │                         │
                    └─────────────────────────┘
```

---

## 7. 上电测试

烧录程序后，数码管应显示以下测试序列：

| 顺序 | 显示内容 | 持续时间 | 说明 |
|-----|---------|---------|------|
| 1 | `888` | 0.5 秒 | 全段测试 |
| 2 | 全灭 | 0.2 秒 | 熄灭测试 |
| 3 | `123` | 0.5 秒 | 数字测试 |
| 4 | `456` | 0.5 秒 | 数字测试 |
| 5 | `000` | 0.3 秒 | 数字测试 |
| 6 | 阈值 (如 `030`) | 持续 | 正常工作 |

---

## 8. 按键操作

| 按键 | 正常模式 | 设置模式 |
|-----|---------|---------|
| **SET (BTN1)** | 进入设置 | 确认并退出 |
| **加 (BTN2)** | 无响应 | 当前位 +1 |
| **减 (BTN3)** | 无响应 | 当前位 -1 |
| **移位 (BTN4)** | 无响应 | 切换位数 (个→十→百) |

**设置流程**：
1. 按 **SET** 进入设置模式（某位闪烁）
2. 按 **移位** 选择要修改的位
3. 按 **加/减** 修改数值
4. 按 **SET** 确认保存

---

## 9. 调试变量

在调试器中可查看以下变量：

| 变量名 | 说明 |
|-------|------|
| `DBG_setValue` | 当前设置值 (0-999) |
| `DBG_setMode` | 0=正常，1=设置 |
| `DBG_setPos` | 0=百位，1=十位，2=个位 |
| `DBG_blinkState` | 闪烁状态 |
| `DBG_seg_d1/d2/d3` | 显示值 (百/十/个位) |

---

## 10. 常见问题

| 问题 | 可能原因 | 解决方法 |
|-----|---------|---------|
| 数码管不亮 | 电源/接线错误 | 检查 VCC/GND/信号线 |
| 显示乱码 | 段码表不匹配 | 确认共阳/共阴极 |
| 按键无响应 | 接线错误 | 检查按键是否接 GND |
| LED 不亮 | 极性接反 | 调换 LED 正负极 |
